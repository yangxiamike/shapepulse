# 手动跟踪市场 V1.2 验收报告

- 验收日期：2026-07-17
- 开发分支：`codex/v1.2`
- 独立 worktree：`C:\Users\hp\.codex\worktrees\v12\手动跟踪市场`
- V1.1 基线提交：`433110d`
- 数据边界：仅使用 `C:\Users\hp\Documents\zer0share` 本地快照。

## Gate 结果

| Gate | 验收内容 | 结果与证据 |
| --- | --- | --- |
| Gate 0 | 仓库、分支、worktree、提交关系和未提交改动只读确认 | V1.1 分支比旧 main 线性领先 5 个提交，工作区均干净；采用 fast-forward，不覆盖用户改动。通过。 |
| Gate 1 | V1.1 合并 main 后的基线 | main 快进至 `433110d`；lint、typecheck、构建、3 个渲染测试、6 个后端测试和隔离状态库下 6 个浏览器测试通过。 |
| Gate 2 | 数据层、筛选语义与快照 | 后端 13 个测试通过，覆盖历史分页、002747 前复权/日期对齐、行业、Top K、市值四种模式和精确快照保存。 |
| Gate 3 | 连续切股、多图、全屏、绘图、历史恢复 | Playwright V1.2 交互用例通过；斐波那契绘制前后像素变化及选择删除均有断言。 |
| Gate 4 | 侧栏一致、底部不遮挡、常用分辨率 | 1600×1000、1366×768、1024×800 均通过几何和无横向溢出断言，并保存截图。 |
| Gate 5 | 全量构建与回归 | lint、typecheck、构建+3 个渲染测试、13 个后端测试、9 个 Chrome 端到端用例全部通过。 |

## 需求映射

| 需求 | 实现位置 | 验证方法 | 结果 |
| --- | --- | --- | --- |
| 完整历史、复权与 OHLCV 日期对齐 | `server/repository.py`、`server/service.py`、`app/lib/api.ts`、`MarketChart.tsx` | 后端真实 002747 集成测试；浏览器检查 2694 根源数据、默认 110/一年 250 根、丢弃 0 根 | 通过 |
| 主动保存整次筛选快照 | `server/state.py`、`server/http.py`、`BoardClient.tsx` | 普通筛选后不写历史；token 精确保存、列表、详情、完整结果和恢复断言 | 通过 |
| 三类形态股票池与连续切股 | `server/service.py`、`MarketClient.tsx` | 选择既有分类、当前项高亮、点击和 ArrowUp/ArrowDown 换股 | 通过 |
| 多图、单图、全屏和 resize | `MarketClient.tsx`、`globals.css` | 1/2/4 图数量、单图放大/恢复、进入/退出全屏断言 | 通过 |
| 绘图与编辑 | `MarketChart.tsx` | 斐波那契实际绘制、选择并 Delete；其余工具共用同一坐标和编辑模型 | 通过 |
| 行业、Top K、市值区间 | `server/service.py`、`BoardClient.tsx` | 行业=电子、市值 50–500 亿元、Top 7 联合筛选；上下限错误阻止提交；后端覆盖四种空值组合 | 通过 |
| 状态区不遮挡 | `globals.css`、`BoardClient.tsx` | 三档分辨率下比较候选区底边与状态卡顶边 | 通过 |
| 全局侧栏一致 | `AppSidebar.tsx`、`globals.css` | 两页侧栏宽高、品牌、导航、底部入口几何逐项相等 | 通过 |

## 自动化记录

```text
pnpm lint                                                     PASS
pnpm typecheck                                                 PASS
pnpm test                                                      PASS（构建 + 3）
python -m unittest server.tests.test_backend                    PASS（13）
pnpm test:e2e                                                  PASS（9）
pnpm exec playwright test tests/e2e/v1.2-acceptance.spec.ts    PASS（3）
```

浏览器结构化结果：`docs/qa/evidence/v1.2/browser-results.json`

## 可视证据

- `docs/qa/screenshots/v1.2/market-1600-002747-full-history.png`：埃斯顿蜡烛、成交量、价格轴与一年默认窗口。
- `docs/qa/screenshots/v1.2/market-1600-pattern-layout-drawing.png`：连续切股后的双图和斐波那契回撤。
- `docs/qa/screenshots/v1.2/board-1600-top.png`：行业、市值区间、Top K 与保存入口。
- `docs/qa/screenshots/v1.2/board-{1600,1366,1024}-state-cards.png`：表格与底部状态区不重叠。
- `docs/qa/screenshots/v1.2/market-{1600,1366,1024}-sidebar.png`：本地行情侧栏回归。

## 已知边界

- 行情新鲜度取决于本机 zer0share 已同步分区；V1.2 不联网补数。
- 筛选 token 有效期为 15 分钟且位于当前服务进程内；过期后客户端需重新筛选再保存。已保存快照持久化在 SQLite，不受 token 过期影响。
- 分时/分钟线、指标库、预警、回放、因子和交易仍不在 V1.2 范围。
