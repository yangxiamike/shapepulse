# Top100 / 模板 / 行情性能与交互 QA 第二轮关闭记录

- 执行时间：2026-07-30
- 实际分支：`codex/top100-performance-ux-v5`
- 前端：`http://localhost:3106`
- 后端：`http://127.0.0.1:8882/api`
- QA 边界：只审查与保存证据；未修改产品、测试或数据代码，未提交。

## 首轮问题关闭

### P1 · 390×844 行情页抽屉遮挡主内容

状态：**已关闭**

- 带模板上下文进入行情页时，右侧抽屉默认关闭。
- 主 K 线在抽屉关闭状态可见。
- 点击“打开右侧面板”后抽屉正常打开。
- 点击抽屉内“关闭”后抽屉关闭，焦点返回“打开右侧面板”按钮。
- 控制台、页面与 HTTP 错误均为 0。
- 截图：`p1-market-mobile-default-closed.png`、`p1-market-mobile-open.png`。

### P2 · 模板库候选标题未明确 Top100

状态：**已关闭**

- `/templates` 候选区标题精确显示“Top100 股票”。
- 控制台、页面与 HTTP 错误均为 0。
- 截图：`p2-templates-top100-heading.png`。

## 最小回归结果

- `1900×956`、`1440×900`、`1024×768`、`390×844` 四种尺寸下，`/template-breadth-v3`、`/templates`、`/templates/new`、`/market` 共 16 个入口全部 HTTP 200。
- 16 个入口均无横向溢出；汇总 console/page/HTTP 错误数为 0。
- 行业宽度：Top100 合计为 100；默认 10 日，可切 20 日；切换后 Treemap 几何不变；“其他行业”为中性色且可展开。
- 新建模板：真实焦点 K 线渲染 124 个可见蜡烛组；完整历史总览可见；缩放和平移均改变视窗；三个边界/整体窗口滑块存在；20–240 与实际交易日提示存在。
- 行情页：键盘切股有效；返回行业宽度链接保留 `template`、`industry=801080`、`window=20`；无“保存模板/另存为模板”入口。
- 快速连续切换行业宽度模板和行情模板后，最终激活项均与最后一次选择一致，未观察到旧响应覆盖当前选择。

## 自动化探针说明

早期试跑中曾出现 `focusBars=0`、`selectedProbe=0`、`preservedReturnContext=false`，原因是 QA 脚本使用了页面并不存在的 `data-bars` 属性、无意义的选中探针，以及命中了全局普通返回链接；不作为产品失败。最终脚本改为统计实际蜡烛组、读取 `aria-pressed`，并定位带模板上下文的返回链接，结果分别为 124、最终模板正确激活、返回参数完整。相关行为也由已通过的定向 E2E `templates.spec.ts` 与 `top100-remediation.spec.ts` 覆盖。

## 残留风险

- 本轮使用本地开发服务和本机 Chrome，绝对首屏耗时仍会受机器状态、首次编译和磁盘缓存影响。
- 本轮验证真实 K 线渲染、缩放、平移、边界控件与限制提示；模板实际保存后的持久化生命周期沿用定向 E2E 覆盖，未在独立 QA 中写入测试模板，以避免改变本地数据。
- 未发现阻断交付的问题。

完整结构化结果见 `browser-results.json` 与 `issues-closure.json`。
