# 变更记录

## 2.7.1 - 2026-07-31

### 新增

- ShapePulse 品牌与面向公开仓库的完整 README，包括功能入口、页面图解、数据准备、研究假设、当前限制、致谢和风险声明。
- 四个冻结模板各自独立的一年期 Top100 行业时间线：覆盖 252 个实际交易日，每 5 日采样并额外保留最新日。
- 时间线支持轨道点击、拖动吸附和左右方向键移动；日期变化时 Treemap、摘要、行业颜色和净扩张/收缩列表同步更新。
- MIT License 与可复现的 README 编号截图生成流程。

### 修复

- 恢复时间线版本的侧边栏页面导航。
- 默认启动页改为带一年时间线的形态宽度页面，并拒绝静默复用不含时间线的旧前端。
- 移除启动脚本中的开发机绝对路径；zer0share 改为通过 `ZER0SHARE_ROOT`、用户 Documents 或同级目录发现。

### 数据边界

- 页面继续只读取用户自己的 zer0share 本地快照，不新增在线行情源。
- V2.7.1 随版本提供 Top100 排名、行业摘要和一年时间线研究快照，不包含 Tushare Token 或本地行情数据库。

## 1.2.0 - 2026-07-17

### 新增

- 行业多选、市值包含边界区间、任意正整数 Top K 与现有板块/ST 条件的组合筛选。
- 用户主动保存的整次筛选快照，包含规则、全部参数、完整结果、排名和匹配度，并支持分页查看与恢复。
- 三类真实形态股票池、当前股票高亮、鼠标选择和键盘上下键连续切股。
- 1/2/4 图布局、单图放大、浏览器全屏和完整 resize 流程。
- 斐波那契回撤、趋势线、线段、射线、水平线、垂直线、曲线、自由绘制，以及选择、调整和删除。

### 修复

- 埃斯顿等股票蜡烛缺失、只见成交量及价格轴与当前价格不一致的问题。
- 时间范围误作历史查询上限的问题；周期范围现在只控制默认可视窗口，完整本地历史可向左浏览。
- 普通筛选自动污染历史、形态分类无结果、底部状态区遮挡表格和两页侧栏重排问题。

### 数据边界

- 继续只读取 `C:\Users\hp\Documents\zer0share` 本地快照；未增加任何在线行情源。
- 前复权分段统一锚定最新本地复权因子，OHLCV 按唯一交易日对齐。

## 1.1.0 - 2026-07-16

### 新增

- 三类形态各自独立 Top 50、综合 Top 50 与完整 50 条结果视图。
- 真实筛选进度、各表日期/警告、前一可比筛选日变化、人工状态抽屉与形态历史。
- 日/周/月/季/年 K 的独立自然周期聚合和默认 110 根日 K。
- 当前股票“形态”标签，包含匹配状态、分数、规则、日期、警告和历史。
- 趋势线、水平线、文本、测量、清除画线、范围与缩放交互。
- 股票详情、搜索换股、自选持久化、错误/空状态和 1024 右栏抽屉。
- 前后端分层计时、bars/详情缓存、Playwright Chrome 功能/性能/视觉验收。

### 修复

- 综合榜与分类榜混用、分类结果未完整接入、分类候选涨跌幅丢失。
- 行情页从 2015 年全量铺开、周期语义重复、周期切换重复请求详情。
- 默认自选无法移除、详情占位、服务重试、首帧时钟 hydration 不一致。
- 1280/1024 图表固定宽度裁切、右栏不可达、候选表和坐标字号过小。
- 重复行情入口、未实现功能伪装可用、固定示例“较昨日”数值。
- 本地启动/停止脚本的残留子进程和端口探测问题。

### 性能

- 首次全市场筛选由基线约 6.654 秒降至最终浏览器端 2.427 秒，并显示真实进度。
- 冷开行情、搜索、换股、首次/缓存周期切换和按钮反馈均达到 v1.1 硬预算。

### 数据边界

- 行情数据源仍严格限定为 `C:\Users\hp\Documents\zer0share` 本地快照。
- 未新增、接入或回退到任何外部行情源。

### 兼容与回滚

- 本地状态库迁移向后兼容，不执行破坏性迁移。
- v1.1 位于 `codex/v1.1`；切回 `main` 即回到 v1.0，zer0share 数据不受影响。
