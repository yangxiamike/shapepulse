# 四模板动态验证 V2：QA 证据

## 结果

- 数据交叉校验：通过。
- 浏览器交互校验：通过。
- 本地 HTTP：200。
- 桌面：1440 × 1000，无横向溢出。
- 手机：390 × 844，无横向溢出。
- 控制台错误：0。
- 页面错误：0。

## 数据校验

- 4 个模板分别只有 1 个冻结阈值，且四个阈值不相同。
- 424 行动态分布与 86 个周频、20 个月末截面完整对应。
- 当前真实合格数量与 `qualified_memberships.csv` 逐模板回勾一致。
- Top5/10/30/100 组合完整，留存和换手均在 `[0, 1]`。
- 六组 Jaccard 共 636 行，组合和取值范围完整。
- 状态迁移矩阵逐行概率和为 1。
- 行业 Wilson 95% 区间全部包含观察合格率。
- 行业、市值、daily_basic 和申万成员均使用对应历史截面。
- 泄漏标记均为 false：未联网、未读 sealed final、未使用未来收益、IC或策略表现。

## 数值容差

模板自匹配处最大 Pearson 为 `1.0000000000000002`，超过 1 的幅度为
`2.22e-16`。QA 使用 `1e-9` 浮点容差；未裁剪分数，未改变阈值和排名。

## 浏览器交互

- 四模板按钮全部可切换，始终只有一个活动面板。
- 动态 Jaccard 可在周频与月末之间切换，六条关系线完整。
- 描述性状态迁移矩阵包含 6 × 6 状态和行列标题，共 49 个单元格。
- 桌面与手机均正确显示
  `statistical validation review / not for model evaluation`。

## 证据文件

- `qa-data-results.json`
- `qa-crosschecks.json`
- `qa-browser-results.json`
- `qa-desktop.png`
- `qa-mobile.png`
