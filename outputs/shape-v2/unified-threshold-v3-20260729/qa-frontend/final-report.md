# V3 前端独立复验报告

- 实际分支：`codex/unified-threshold-app-v3`
- 复验 HEAD：`7f57b3f`
- 复验时间：2026-07-29
- 数据边界：本机 `C:\Users\hp\Documents\zer0share`；健康检查为 `network: not used`
- 状态库：复用 `qa-market-state.sqlite3`
- 有效前端：`http://localhost:3101`（3100 为上一轮遗留进程，本报告不采用其页面）

## 结论

第一轮提出的 2 个 P1、7 个 P2 均已关闭；本轮未发现阻断交付的新增问题。三个主页面在 1440×1000、1024×800、390×844 共 9 个组合均完成修复后截图，未见页面级横向溢出、HTTP 失败、控制台错误或页面异常。

## 逐项关闭

1. **P1 曲线口径不一致：关闭。** 行情页双曲线均为前复权 log-close 后窗口内独立 z；SVG 标记为 `qfq-log-close-independent-z`，模板/候选均值为 0、标准差为 1，两条曲线均有完整纵向变化，不再出现模板曲线被压扁。
2. **P1 390 模板抽屉不可操作：关闭。** 合法 `template=fresh_breakout` URL 自动激活“模板”并打开视口内抽屉；选择模板、点股票、关闭抽屉均使用普通点击，不依赖 force 或 DOM 注入。
3. **P2 template URL 不自动打开模板页签：关闭。** 桌面与移动端均自动激活模板页签并保留 URL 参数。
4. **P2 最近变化漏掉“当前 0、但 5 日有变化”的行业：关闭。** 最近变化按 `abs(change_5d)` 取面积，四模板非零变化行业均进入图块；收缩到 0 的行业仍显示。
5. **P2 缺少最大扩张/收缩摘要：关闭。** 摘要按每模板全部 31 个申万一级行业计算，页面值与原始行业表逐模板一致。
6. **P2 390 行情工具小且被截：关闭。** 周期、绘图、区间工具采用 44px 触控目标；横向工具条可滚到末端，最后一项可达。
7. **P2 390 宽度页模板标签被截：关闭。** 四模板固定为清晰的 2×2 网格，无横向溢出。
8. **P2 390 模板库底部内容被导航遮挡：关闭。** 主区保留底部安全间距，末项可滚到固定导航上方。
9. **P2 可见但无效的帮助/退出按钮：关闭。** 三个页面均不再出现可点击的帮助/退出死入口。

## 证据

- 桌面/平板/手机：`after-templates-{1440,1024,390}.png`、`after-market-{1440,1024,390}.png`、`after-breadth-{1440,1024,390}.png`
- 双曲线：`after-market-template-1440.png`
- 复验脚本：`run-final-visual-qa.mjs`
- 结构化结论：`issues-final.json`

## 启动时序补充复验

针对“行情启动加载可能关闭刚自动打开的模板抽屉”做了独立窄复验。390×844 打开 `/market?code=603986&template=fresh_breakout` 后，先等待行情完成加载且模板股票列表完整返回 200 只，再执行检查：

- 抽屉仍为 `open`，位置 `left=12`、`right=390`、宽度 378px，完整位于视口内。
- 抽屉为 `visibility: visible`、`pointer-events: auto`。
- 普通点击首股成功从 `603986` 跳到 `301234`，URL 保留 `template=fresh_breakout`，抽屉仍保持打开和可操作。
- 普通点击关闭后，抽屉变为关闭、隐藏且不再接收点击。
- HTTP 200；控制台/页面错误 0；失败响应 0。

结论：启动加载与模板抽屉自动打开之间的时序问题已关闭。

## 残留

P1：0；P2：0。未改产品代码，未使用联网行情，未读取 sealed final。
